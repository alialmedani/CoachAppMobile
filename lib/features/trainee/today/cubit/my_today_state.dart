part of 'my_today_cubit.dart';

/// Boilerplate-driven (GetModel owns async UI state), so a single initial
/// state suffices.
abstract class MyTodayState {}

class MyTodayInitial extends MyTodayState {}
