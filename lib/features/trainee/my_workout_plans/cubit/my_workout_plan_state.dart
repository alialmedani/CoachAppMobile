part of 'my_workout_plan_cubit.dart';

/// The trainee workout-plan screens are boilerplate-driven (GetModel owns the
/// async UI state), so a single initial state is all this cubit needs.
abstract class MyWorkoutPlanState {}

class MyWorkoutPlanInitial extends MyWorkoutPlanState {}
